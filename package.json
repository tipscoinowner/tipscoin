{
  "name": "tipscoin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "hardhat": "hardhat",
    "deploy": "npx hardhat run scripts/deploy.ts --network localhost",
    "populate": "npx hardhat run scripts/demoPopulate.ts --network localhost",
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "start": "cd app && npm start"
  },
  "devDependencies": {
    "hardhat": "2.28.0",
    "@nomicfoundation/hardhat-toolbox": "3.0.0",
    "ethers": "^6.8.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.1",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
